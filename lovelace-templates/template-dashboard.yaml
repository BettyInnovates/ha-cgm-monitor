title: CGM Monitor
icon: mdi:diabetes

# Pull in all button-card templates so they're available on every view.
# In YAML-mode Lovelace (ui-lovelace.yaml) this path is relative to the
# config/ directory, so adjust if your file lives elsewhere.
button_card_templates: !include lovelace-templates/button-card-templates.yaml

views:
  # add your views here, e.g.:
  # - !include lovelace-templates/template-overview.yaml
  # - !include lovelace-templates/template-states-overview.yaml
  # - !include lovelace-templates/template-subject-detail.yaml
